export enum SIZES {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg',
}

export enum TITLES_BUTTON {
  ADDED = 'Added',
  ADD = 'Add to card',
}

export enum TOGGLE_SELECT_TYPES {
  SELECT = 'select',
  REMOVE = 'remove'
}
